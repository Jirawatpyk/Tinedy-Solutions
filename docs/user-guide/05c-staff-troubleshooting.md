# แนวทางการแก้ไขปัญหา (Troubleshooting) สำหรับ Staff

เอกสารอ้างอิงสำหรับแก้ไขปัญหาที่พบบ่อยเมื่อใช้งาน Tinedy CRM

---

## ปัญหาทั่วไป

### 🔴 ไม่สามารถเข้าสู่ระบบ

**อาการ:**
```
Error: Invalid email or password
หรือ
Error: Connection failed
```

**วิธีแก้:**

#### **1. ตรวจสอบ Email/Password**
- ✅ Caps Lock ปิด? (A ≠ a)
- ✅ Space ท้าย Email? ("  user@mail.com  " ❌)
- ✅ Password พิมพ์ถูก? (หมวกถูกไหม)
- ✅ ยืนยันรหัสผ่าน ก่อนล็อกอิน

**วิธี:**
```
1. ลบ Email → พิมพ์ใหม่ (ตรวจสอบตัวอักษร)
2. ลบ Password → พิมพ์ใหม่ (ตรวจสอบ Caps Lock)
3. Click [Sign In]
```

#### **2. ลืมรหัสผ่าน**
- ❌ ปุ่ม "Forgot Password" ยังไม่ใช้งาน
- ✅ **ทางแก้:** ติดต่อ Admin/Manager
  - Chat: "Can you reset my password?"
  - Or: โทรศัพท์

#### **3. ตรวจสอบ Internet Connection**
```
WiFi/Mobile Data ปกติไหม?
├─ Google.com เปิดได้? (ถ้าได้ = ปกติ ✅)
└─ ถ้าไม่ได้ = เปลี่ยน WiFi/Network
```

#### **4. ลองเบราว์เซอร์อื่น**
- Chrome → Safari/Edge/Firefox
- ปัญหา Browser cache

#### **5. ลบ Browser Cache**
- Windows: Ctrl+Shift+Delete
- Mac: Cmd+Shift+Delete
- เลือก "All time"
- Click "Delete data"
- ปิด-เปิดเบราว์เซอร์ใหม่

---

### 🔴 Dashboard โหลดช้า / Timeout

**อาการ:**
```
Loading... (สิ้นสุดไม่มีที่สิ้นสุด)
หรือ
Error: Request timeout
```

**วิธีแก้:**

#### **ระดับ 1 - ตรวจสอบ Internet**
```
Internet speed ≥ 1 Mbps?
├─ ช้า < 1 Mbps?
│  └─ เปลี่ยน WiFi
│     หรือเข้าใกล้ router
└─ ปกติ ≥ 1 Mbps? ไปขั้นต่อไป
```

#### **ระดับ 2 - Refresh Page**
```
1. Press F5 (Refresh)
2. Wait 3-5 seconds
3. Still loading? → Go next step
```

#### **ระดับ 3 - ลบ Cache ลึก**
```
1. Press Ctrl+Shift+Delete (Windows) / Cmd+Shift+Delete (Mac)
2. Select "All time"
3. Check:
   - Cookies
   - Cached images/files
4. Click "Delete data"
5. Close browser tab
6. Open new tab → Login again
```

#### **ระดับ 4 - ลอง Browser อื่น**
```
Chrome ❌ ช้า?
└─ Lỏง Firefox / Safari / Edge
```

#### **ระดับ 5 - เพิ่มเติม**
```
1. ตรวจสอบ Extensions ปิด?
   (Add-ons/Extensions อาจ Slow)
2. ลอง Incognito mode (Ctrl+Shift+N)
   (ไม่มี cache/extensions)
3. Restart browser ทั้งหมด
```

#### **ระดับ 6 - Contact Support**
```
Chat Manager: "Dashboard loading very slow"
Attach screenshot + time
```

---

### 🔴 ปุ่ม Start/Complete/Cancel ไม่ทำงาน

**อาการ:**
```
คลิก [Start] แล้ว ไม่มีอะไรเกิดขึ้น
หรือ
Spinner loading ไม่จบ
```

**วิธีแก้:**

#### **ตรวจสอบ 1: Modal ถูก?**
```
Booking Detail Modal เปิดถูกไหม?
├─ ❌ ถ้าเปิด Card ธรรมดา
│  └─ ปิด → Click Card ใหม่
└─ ✅ ถ้า Modal เปิด → ถัดไป
```

#### **ตรวจสอบ 2: ปุ่มมี Disable?**
```
ปุ่มเป็นสีเทา (disabled)?
├─ ❌ เป็นสีเทา
│  └─ สถานะอาจไม่รองรับ
│     เช่น Completed → ไม่มี [Start]
└─ ✅ ปกติ → ไปขั้นต่อไป
```

#### **ตรวจสอบ 3: Internet Connection**
```
1. WiFi ปกติไหม? (ลองเปิด Google)
2. ถ้าช้า → เปลี่ยน WiFi
3. ถ้าปกติ → ไปขั้นต่อไป
```

#### **ตรวจสอบ 4: Refresh & Try again**
```
1. Press F5 (Refresh Dashboard)
2. Click Card ใหม่
3. Try [Start] again
4. Wait 5 seconds
```

#### **ตรวจสอบ 5: Browser Dev Tools**
```
1. Press F12 (Open Dev Tools)
2. Click "Console" tab
3. Do you see red errors?
   ├─ ❌ Yes error → Screenshot → Send to Manager
   └─ ✅ No error → ไปขั้นต่อไป
```

#### **ตรวจสอบ 6: Clear Browser Cache & Try**
```
1. Ctrl+Shift+Delete
2. "All time"
3. Check "Cookies" + "Images/Files"
4. "Delete data"
5. Close tab
6. Open new tab → Login → Try again
```

#### **ตรวจสอบ 7: ลอง Browser อื่น**
```
Chrome ❌ ไม่ทำงาน?
└─ Try Firefox / Safari / Edge
```

#### **ถ้ายังไม่ได้:**
```
Chat Manager:
"[Start] button not responding"
+ Screenshot
+ Browser name (Chrome/Firefox)
+ What error you see (if any)
```

---

### 🔴 ข้อมูล Notes/หมายเหตุ หายไป

**อาการ:**
```
พิมพ์ Notes แล้วปิด Modal
เปิดใหม่ → Notes หาย!
```

**วิธีแก้:**

#### **สาเหตุ 1: ไม่ Save**
```
ตรวจสอบ:
├─ มี [Save] button?
│  └─ ✅ Yes → Click Save ต้อง
├─ Auto-save?
│  └─ ❓ ตรวจสอบ 3-5 วินาที
└─ หลังจาก Save → ปิด Modal → เปิดใหม่
```

#### **สาเหตุ 2: Internet หาย**
```
ตรวจสอบ:
├─ WiFi ตัด?
│  └─ จะ Save ไม่สำเร็จ
└─ ก่อน Save → ตรวจสอบ WiFi ก่อน
```

#### **สาเหตุ 3: Unicode/Special Characters**
```
ตัวอักษรพิเศษใน Notes?
├─ 🎉 Emoji
├─ 你好 Chinese
├─ العربية Arabic
└─ ลองพิมพ์ Simple text ก่อน (a-z, 0-9)
```

#### **วิธีแก้:**
```
1. Refresh (F5)
2. Click Card ใหม่
3. ตรวจสอบ Notes

ถ้าหายจริง:
4. พิมพ์ Notes ใหม่
5. คลิก [Save] (ชัดเจน)
6. รอ 3 วินาที (Spinner จะหาย)
7. ปิด Modal
8. เปิด Card ใหม่ → ตรวจสอบ
```

---

### 🔴 Chat ข้อความไม่ส่ง

**อาการ:**
```
พิมพ์ข้อความ คลิก Send
ข้อความไม่ปรากฏใน Chat
```

**วิธีแก้:**

#### **ตรวจสอบ 1: Internet Connection**
```
WiFi/Network ปกติ?
├─ ❌ Connection lost
│  └─ Reconnect WiFi
│     แล้ว Try ใหม่
└─ ✅ ปกติ → ไปขั้นต่อไป
```

#### **ตรวจสอบ 2: Message ว่าง?**
```
ตรวจสอบ:
├─ Typing พื้นที่ Input?
│  └─ ❌ ว่าง → พิมพ์อะไรสักอย่าง
└─ ✅ มีข้อความ → ไปขั้นต่อไป
```

#### **ตรวจสอบ 3: File ใหญ่?**
```
กรณี Upload file:
├─ File size ≤ 10 MB?
│  └─ ❌ > 10 MB → ใช้ file เล็กกว่า
└─ ✅ ≤ 10 MB → ไปขั้นต่อไป
```

#### **ตรวจสอบ 4: Manager ออนไลน์ไหม?**
```
ชื่อ Manager มีสีเขียว?
├─ ❌ Offline (สีเทา) → OK, message จะส่ง
└─ ✅ Online (สีเขียว) → ไปขั้นต่อไป
```

#### **ตรวจสอบ 5: Refresh Chat**
```
1. Refresh (F5)
2. เข้า Chat ใหม่
3. ดู Message → ได้ไหม?
   ├─ ❌ ยังไม่ได้ → ไปขั้นต่อไป
   └─ ✅ ได้ (ล่าช้า) → OK
```

#### **ตรวจสอบ 6: ลอง Message ใหม่**
```
1. เลือก Manager ใหม่
2. พิมพ์: "Test message"
3. Click Send
4. ได้ไหม?
   ├─ ✅ Yes → ก่อนหน้า message หาย
   └─ ❌ No → ไปขั้นต่อไป
```

#### **ถ้ายังไม่ได้:**
```
Chat different Manager:
"Chat system not working. Can't send to [name]"
```

---

### 🔴 Calendar Events ไม่อัพเดท

**อาการ:**
```
Dashboard เห็นงาน
Calendar ไม่เห็นงาน (หรือเห็นเก่า)
```

**วิธีแก้:**

#### **ขั้นที่ 1: Refresh Calendar**
```
1. Click Calendar menu
2. Page loads
3. ≠ Check Calendar → ดูงานใหม่
```

#### **ขั้นที่ 2: ลบ Cache ลึก**
```
1. Ctrl+Shift+Delete
2. "All time"
3. Check: Cookies, Images, Files
4. Click "Delete data"
5. Close browser
6. Open new tab → Calendar
```

#### **ขั้นที่ 3: Check Booking Status**
```
ตรวจสอบ:
├─ Dashboard เห็นอะไร?
│  └─ Status = ?
├─ Calendar colored ถูก? (🟢🔵🟡 ?)
└─ Color = Status
   🟢 = Completed
   🔵 = In Progress
   🟡 = Pending
```

#### **ขั้นที่ 4: Internet Connection**
```
WiFi/Network ปกติ?
├─ ❌ Slow/Lost → Reconnect
└─ ✅ OK → ไปขั้นต่อไป
```

#### **ขั้นที่ 5: ปิด-เปิด Tab**
```
1. Close Tab ปฏิทิน
2. Wait 2 seconds
3. Open new tab
4. Login ใหม่ → ไป Calendar
5. Check → Events ใหม่หรือยัง
```

---

### 🔴 Notification ไม่มา (ไม่ได้รับแจ้งเตือน)

**อาการ:**
```
ได้งานใหม่ แต่ไม่มี notification
```

**วิธีแก้:**

#### **ตรวจสอบ 1: Setting Enabled?**
```
Profile → Notification Settings
├─ Message Notifications: ✅ On?
├─ New Booking Alert: ✅ On?
├─ Chat Sound: ✅ On?
└─ ถ้า ❌ ปิด → Click On
```

#### **ตรวจสอบ 2: Browser Permission**
```
Browser notification access?
├─ Chrome: 3 dots → Settings → Privacy → Notifications
├─ Firefox: Menu → Preferences → Privacy → Notifications
└─ Check: Tinedy CRM = "Allow"
```

#### **ตรวจสอบ 3: Browser Notification Test**
```
1. ไปที่ Browser settings
2. Test notification
3. แจ้งเตือนมาไหม?
   ├─ ❌ ไม่มา → Browser problem
   └─ ✅ มา → ไปขั้นต่อไป
```

#### **ตรวจสอบ 4: Quiet Hours**
```
Profile → Notification Settings
Quiet Hours: 10 PM - 7 AM
├─ ปัจจุบันเป็น Quiet Hours?
│  └─ ✅ Yes → Notifications OFF (normal)
└─ ❌ No → ไปขั้นต่อไป
```

#### **ตรวจสอบ 5: Device Volume**
```
├─ Phone/Computer Volume up?
├─ Mute mode ปิด?
└─ ✅ ทั้งคู่ on → ไปขั้นต่อไป
```

#### **ตรวจสอบ 6: Request Permission**
```
Dashboard → FAB (Floating Action Button)
└─ [Request Notification] ปุ่ม
   └─ Click → Grant permission
```

---

## Network & Connection Issues

### 🔴 Internet Disconnects บ่อย

**อาการ:**
```
Loading spinner บ่อยครั้ง
Connection lost / Timeout บ่อย
```

**วิธีแก้:**

#### **ตรวจสอบ WiFi**
```
1. WiFi signal ดีไหม? (ลูก 4/5?)
2. ลองเข้าใกล้ Router
3. ลองเปลี่ยน WiFi channel (ผ่าน Router admin)
4. Restart router:
   - Unplug 10 seconds
   - Plug back in
   - Wait 1 minute
```

#### **ตรวจสอบ Mobile Data**
```
ใช้ 4G/5G?
├─ ❌ Signal ไม่ดี → เปลี่ยนที่ตั้ง
├─ ✅ Signal ดี → ไปขั้นต่อไป
└─ ❓ Data quota หมด? (Unlimited ไหม?)
```

#### **ลอง Wired Connection**
```
ต้องใช้ Desktop?
└─ Ethernet cable → ทดสอบ
   (Wired ≠ Wireless ปัญหา)
```

---

## Account & Security Issues

### 🔴 บัญชี Lock / Suspended

**อาการ:**
```
Error: Account locked
หรือ
Error: Account suspended
```

**วิธีแก้:**
```
Contact Manager/Admin immediately:
- Chat: "My account is locked. Help!"
- Phone: [number]

Provide:
- Email address
- Staff number (STFxxxx)
- Error message (screenshot)
```

---

### 🔴 Logged Out เอง

**อาการ:**
```
ทำงานอยู่ → เดี๋ยวสั้น
Log out เอง
```

**วิธีแก้:**

#### **สาเหตุ 1: Session Timeout**
```
Session หมดอายุ: 24 ชั่วโมง
│
├─ ป้องกัน:
│  └─ Logout ก่อนนอน
└─ แก้:
   └─ Login ใหม่
```

#### **สาเหตุ 2: Browser Cookies ลบเอง**
```
Browser settings:
├─ Clear cookies on exit?
│  └─ Turn OFF
└─ ถ้า enable → cookies หาย ทุกครั้งปิด
```

#### **สาเหตุ 3: Incognito Mode**
```
ใช้ Incognito/Private window?
├─ ✅ Yes → Normal mode ใช้ (Incognito = temp)
└─ ❌ No → ไปขั้นต่อไป
```

#### **สาเหตุ 4: Multiple Devices/Tabs**
```
Login หลายอุปกรณ์?
├─ Device A + Device B + Device C
│  └─ หนึ่ง logout ได้ทั้งหมด
└─ แก้:
   └─ Login อุปกรณ์ละอัน
```

---

## Performance Issues

### 🔴 ระบบช้ามาก

**อาการ:**
```
ทุกอย่างโหลดช้า
Buttons ช้าในการตอบสนอง
```

**วิธีแก้:**

#### **ทำความสะอาด Browser**
```
1. Ctrl+Shift+Delete → All time
2. Select all options
3. Delete data
4. Close browser
5. Reopen & Login
```

#### **ลบ Extensions**
```
Chrome: 3 dots → More tools → Extensions
├─ Disable all ที่ไม่จำเป็น
├─ Test → ดี?
└─ ✅ Yes → Keep disabled
```

#### **ใช้ Lite Browser**
```
ลดลง RAM usage:
├─ Firefox (lighter than Chrome)
├─ Safari (Mac)
├─ Edge (Windows)
└─ Test again
```

#### **Close Other Tabs**
```
ปิด Tabs อื่น ๆ ที่ไม่ใช้
├─ RAM available ↑
└─ Performance ↑
```

---

## Payment & Financial Issues

### 🔴 Earnings ไม่ถูกต้อง / Earnings ไม่เห็น

**อาการ:**
```
Stats "Earnings": ฿0
หรือ
Earnings ต่ำเกินไป
```

**วิธีแก้:**

#### **ตรวจสอบ 1: Job Status**
```
Job ต้อง COMPLETED:
├─ Status = Completed? ✅
├─ Status = In Progress? ❌ (ยังไม่ได้)
├─ Status = Pending? ❌ (ยังไม่เริ่ม)
└─ Mark as Complete → Earnings update
```

#### **ตรวจสอบ 2: Service Package**
```
Service Package มี price?
├─ ❌ Free/No price → Earnings = ฿0
└─ ✅ Have price → คำนวณจำนวน
```

#### **ตรวจสอบ 3: Refresh Data**
```
1. Refresh (F5)
2. Wait 3-5 seconds
3. Check Earnings again
4. ถ้า update:
   └─ Realtime delay (OK)
```

#### **ตรวจสอบ 4: Payment Status**
```
Payment status?
├─ Unpaid: ไม่คำนวณ Earnings?
│  └─ ตรวจสอบ Manager
└─ Paid: Earnings ✅ count
```

#### **ถ้าไม่เข้าใจ:**
```
Chat Manager:
"Earnings shows ฿0, but I completed 5 jobs?"
+ Screenshot
```

---

## Server/System Issues

### 🔴 Maintenance / System Down

**อาการ:**
```
Error: Service Unavailable (503)
หรือ
Error: Server Error (500)
```

**วิธีแก้:**
```
Server maintenance อยู่
├─ ✅ ปกติ 1-2 ชั่วโมง
└─ รอ 15-30 นาที แล้วลอง Login ใหม่
```

**ถ้ายาว (> 2 ชั่วโมง):**
```
Chat Manager: "Server down?"
```

---

## คำแนะนำการประเลิกปัญหา

### ✅ ก่อนติดต่อ Support

**เตรียมข้อมูล:**
1. **Browser name:** Chrome? Firefox?
2. **Device:** Desktop? Mobile?
3. **Error message:** Screenshot
4. **Booking ID:** BK... (ถ้าเกี่ยวข้อง)
5. **Timestamp:** เวลาที่เกิด
6. **Steps:** ทำอะไรก่อนเกิด

### 📝 ข้อมูลที่ Manager/Admin ต้องการ

```
Chat Message:
---
Issue: [ชื่อปัญหา]
Browser: [Chrome/Firefox]
Device: [Desktop/Mobile]
Screenshot: [ใหม่]
Booking: [BK...]
Time: [10:30 AM]
Steps: [1. Click... 2. Type... 3. Error]
---
```

### 📞 ติดต่อ Support

```
Priority:
1. Chat Manager (ทันที)
2. Call Admin (ด่วน)
3. Email (ธรรมดา)

Response time:
- Chat: 30 min - 2 hours
- Call: < 30 min
- Email: 1-2 days
```

---

**Last Updated:** 10 มกราคม 2568

*เอกสารนี้จัดทำเพื่อช่วยแก้ไขปัญหาที่พบบ่อยสำหรับ Staff*
